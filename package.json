{
  "name": "add-reviewer-labels",
  "version": "0.0.1",
  "description": "A GitHub Action to add labels to a PR based on assigned reviewers",
  "author": "Franz See <franz@see.net.ph>",
  "license": "MIT",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "engines": {
    "node": "v20.11.1"
  },
  "keywords": [
    "github",
    "github action",
    "code review",
    "pull request",
    "reviewers",
    "labels",
    "tags"
  ],
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@actions/github": "^6.0.0"
  }
}
